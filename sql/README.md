-- Exemplos de SQL deste projeto (Postgres).
-- São versões didáticas do que roda em produção — sem dados reais, sem credenciais.
-- Nomes de tabela/coluna batem com o modelo usado no cockpit (snapshots SIAA → JSONB).

-- 01_matriculas_periodo.sql  → universo de matrículas no período (DISTINCT ON + datas)
-- 02_ranking_recuperacao_siaa.sql → RGMs que sumiram do último relatório mas ainda contam
-- 03_premiacao_metas_equipe.sql → metas/R$ por equipe na mesma campanha
